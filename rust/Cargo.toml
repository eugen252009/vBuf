[package]
name = "vbuf-core"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde_json = "1.0"
uuid = { version = "1.0", features = ["v4"] }
hex = "0.4"
xxhash-rust = { version = "0.8.15", features = ["xxh3"]}

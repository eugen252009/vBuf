[workspace]
members = ["vbuf-layout", "vbuf-ml", "vbuf-runtime"]

[package]
name = "vbuf-core"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
libc = "0.2.186"
memmap2 = "0.9"
vbuf-layout = { path = "vbuf-layout" }
rayon = "1.10"
prost = "0.13"

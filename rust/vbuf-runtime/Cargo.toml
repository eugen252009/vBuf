[package]
name = "vbuf-runtime"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib", "cdylib"]

[features]
default = []
ggml = []
cuda = []

[dependencies]
memmap2 = "0.9"
vbuf-core = { path = ".." }
vbuf-ml = { path = "../vbuf-ml" }
sha2 = "0.10"

[build-dependencies]
